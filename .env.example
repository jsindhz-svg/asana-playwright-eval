ASANA_USERNAME=admin
ASANA_PASSWORD=password123
#sha256-hash-of-your-password
ASANA_PASSWORD_HASH=ef92b778bafe771e89245b89ecbc08a44a4e166c06659911881f383d4473e94f

BASE_URL=https://create-asana-like-pr-39y5.bolt.host

# Do not commit the live password to source control.
# Provide the real plaintext password only at runtime via a secure secret manager,
# or pass it directly to login() when the test runs. The hash is for validation only.
